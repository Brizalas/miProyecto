1. Mostrar una lista de objetos en HTML
   Ya lo has hecho con ArrayList + Model + data-th-each.

2. Crear un formulario HTML
   Para que el usuario pueda escribir datos desde el navegador.

3. Enviar ese formulario al Controller
   Usando un método POST con @PostMapping.

4. Recibir los datos del formulario en Java
   Primero de forma sencilla con @RequestParam.

5. Añadir el nuevo objeto a la lista
   Por ejemplo, crear una nueva Guitarra y meterla en el ArrayList.

6. Redirigir otra vez a la lista
   Para ver el nuevo dato pintado en pantalla.

7. Más adelante:
   - borrar objetos
   - editar objetos
   - separar Controller / Service / Repository
   - conectar con H2
   - usar Entity y JPA
